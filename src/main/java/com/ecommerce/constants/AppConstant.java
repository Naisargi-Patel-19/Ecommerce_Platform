package com.ecommerce.constants;

import java.io.File;
import java.nio.file.Files;

public class AppConstant {
    public static final String PROFILE_IMAGE_PATH = System.getProperty("user.dir") + File.separator + "profileImages" + File.separator;
    public static final String PRODUCT_IMAGE_PATH = System.getProperty("user.dir") + File.separator + "productImages";
}
